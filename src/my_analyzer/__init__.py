"""my_analyzer: a datatrawl analyzer project started from the WVURAIL template.

The analyzer itself lives in `my_analyzer.analyzer`; the entry point declared in
`pyproject.toml` points there, so installing this package makes the analyzer
visible to `datatrawl list analyzers` with no `--plugin` flag.
"""
